
export function JsonToJsonBom(build_bom_nested){
    console.log("build_bom_nested 2", build_bom_nested)
    // Test
    // const build_bom_new = build_bom_nested.unit1.map((unit)=>(unit))

    // inserting shipping splits 
    


    const build_bom = build_bom_nested
    return build_bom
}